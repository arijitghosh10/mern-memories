import { makeStyles } from "@mui/styles";

export default makeStyles(()=>({
    paper:{
        marginTop: '1rem',
        padding: 15,
    },
    form:{
        display:'flex',
        justifyContent:'center',
        flexDirection:'column',
        gap:'0.75rem 0',
    },
}))